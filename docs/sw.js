/* generated by build_site.py */
const CACHE = 'baseball-records-6c3265eacc8c';
const ASSETS = ["./", "index.html", "style.css", "app.js", "api-local.js", "manifest.json", "icon-180.png", "icon-512.png", "data/people.csv", "data/batting.csv", "data/pitching.csv", "data/fielding.csv", "data/teams.csv", "data/awards.csv", "data/allstar.csv", "data/hof.csv", "data/seriespost.csv", "data/franchises.csv", "data/franchise_eras.csv"];

self.addEventListener('install', (e) => {
  e.waitUntil(caches.open(CACHE).then((c) => c.addAll(ASSETS)).then(() => self.skipWaiting()));
});

self.addEventListener('activate', (e) => {
  e.waitUntil(caches.keys().then((keys) =>
    Promise.all(keys.filter((k) => k !== CACHE).map((k) => caches.delete(k)))
  ).then(() => self.clients.claim()));
});

self.addEventListener('fetch', (e) => {
  if (e.request.method !== 'GET') return;
  e.respondWith(
    caches.match(e.request, { ignoreSearch: true }).then((hit) =>
      hit || fetch(e.request))
  );
});
